<?php

namespace App\Http;

use Illuminate\Foundation\Http\Kernel as HttpKernel;

/**
 * HTTP kernel — middleware is configured in bootstrap/app.php via Application::configure()->withMiddleware().
 */
class Kernel extends HttpKernel
{
    //
}
