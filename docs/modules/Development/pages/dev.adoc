=== Werkwijze

include::quick-reference.adoc[]

=== Local Development Setup

Stappen voor de eerste keer:

 * Clone de repository
 * Kopieer .env.example naar .env en pas deze aan indien nodig
 * Zet TELESCOPE_ENABLED=false, of zie Telescope Monitoring
 * run composer install
 * run yarn install
 * run ./start-dev.sh
 * run sail artisan key:generate (niet zeker, probeer eerst zonder. Als je dit doet, vergeet .env niet aan te passen (APP_KEY).
 * run sail artisan migrate --seed of met reset.sh (inclusief data import)
 * voeg toe aan je host file (etc/hosts):

[source,bash]
----
# Local development domains (added by setup-local-hosts.sh)
127.0.0.1 sso.local.privatescan.nl
127.0.0.1 crm.local.privatescan.nl
127.0.0.1 forms.local.privatescan.nl
127.0.0.1 logs.local.privatescan.nl
127.0.0.1 phpmyadmin.local.privatescan.nl
127.0.0.1 suitecrm.local.privatescan.nl
127.0.0.1 n8n.local.privatescan.nl
----

 * Open in browser: http://crm.local:8000

Stappen voor een volgende keer:

 * run ./start-dev.sh
 * Open in browser: http://crm.local:8000


=== Feature testen uitvoeren

* sail artisan test --parallel --colors=always

Of in PHPStorm

=== UI Testen uitvoeren
* run ./test-ui.sh

=== Frontend development

Lokaal ontwikkelen maken we gebruiken van link:https://vite.dev/[Vite] in combinatie met link:https://laravel.com/docs/12.x/installation[Laravel] Mix.
./start-dev.sh start deze vite servers op (2).
vite

De stack is een combinatie van Blade en Vue templates. Zo heeft Krayin zijn templates, en hebben we een eigen directory met templates:

* Kryain: packages/Webkul/Admin/src/Resources/views
* custom: resources/views/adminc

Adminc is in Laravel geregistreerd als een extra view namespace, daarmee kun je deze templates op deze manier includen:
```
<x-adminc::anamnesis.card
:anamnesis="$anamnesisItem"
show-created-date="true"
/>
```
(Syntax met ':' zorgt voor binding van variabelen, anders statische waarde)
Deze syntax heeft trouwens de voorkeur, ten opzichte van de @include syntax.

Door vite, zou je in theorie geen last moeten hebben van cache problemen. Mocht je dit toch hebben, dan kun je de browser cache legen of in incognito mode werken. Daarnaast kun je ook de Laravel cache legen: sail artisan optimize:clear

Note: Deze visie is wat later in het project gekomen, niet alles is op deze manier nu opgezet. Zo af en toe verplaats ik nog templates naar adminc.


Verder gebruiken we in deze stack:

* Laravel Mix (Webpack) voor asset bundling
* Tailwind CSS voor styling
* Vue.js voor interactieve UI componenten

Om de frontend te bouwen, volg deze stappen:

1. Installeer Node.js en npm (indien nog niet geïnstalleerd):
- Download en installeer vanaf https://nodejs.org/
2. yarn of npm dependencies installeren:
- yarn install
3. Voor vite: starten we lokaal een vite server -> yarn dev (zie ook start-dev.sh)

note:
Dit is al gedaan, hoef je niet opnieuw te doen. Was noodzakelijk voor de combi met dit project.
yarn config set nodeLinker node-modules


=== Migratie script opschonen

Na verloop van tijd zal het aantal migratie files groeien. Dit kan ervoor zorgen dat het opzetten van een nieuwe omgeving lang duurt. Laravel biedt de mogelijkheid om de huidige database structuur te dumpen naar een sql file en alle migratie files te verwijderen.

1. run sail artisan schema:dump (zonder --prune)
2. doe git add op de nieuw dump file (zie in database de sql file).
3. Voeg deze toe aan Git

Normaal gesproken zou je liever met de optie --prune willen draaien, zodat de migratiescripts ook worden verwijderd.
In dit geval draaien de testcases echter op SQLite, omdat dit de uitvoering van de tests aanzienlijk versnelt.
Het nadeel daarvan is dat SQLite niet werkt met de database dump file.
Daarom gebruiken de testcases in dit scenario wél de migratiescripts.

=== Migratie instructies

==== Samenvatting

Je dient een mysql dump te maken vanuit de oude server, export met Filezilla te downloaden naar de Citrix omgeving, dan kopiëren van Citrix omgeving naar lokaal. Van lokaal met scp naar de nieuwe server en dan importeren in de mysql docker.

Beter is rechtstreeks van de oude server naar de nieuwe met scp, maar er zijn nog firewall issues.

==== Stappen plan om een nieuwe prod suite CRM dataset klaar te zetten

1. **MySQL dump vanuit SSH sessie oude server**
- Run `./export_db-suitecrm_prod.sh` (note: bestand staat in /home/mbulthuis)
- wachtwoord voor user mark, zie keeper 'Sugar suite - PphMyAdmin - user mark'
- Na db export, gaat deze copieren met scp naar de nieuwe test server. Met gebruiker mbulthuis, dit wachtwoord opgeven.

2. **Uitpakken**
- `gunzip -c scrm_privatesuite9_2025-09-02_1620.sql.gz > scrm_privatesuite9_2025-09-02_1620.sql`

3. **Database aanmaken** [optioneel, alleen eerste keer]
- https://phpmyadmin.dev.privatescan.nl -> maak database aan met naam `sugarcrm_privatesuite9`

4. **Importeren in MySQL docker**
- `sudo docker exec -i mysql mysql -u root -p[DB_PASSWORD] scrm_privatesuite9 -h 127.0.0.1 -P 3306 < ./scrm_privatesuite9_2025-09-02_1620.sql`
(DB_PASSWORD is te vinden in .env.prod DB_PASSWORD, van Github Project Krayin-laravel-crm)

Note: dit is exclusief de e-mail attachments, zie daarvoor rsync command.

=== Commands

include::cmd/cmd-import-sugarcrm-contacts.adoc[]

include::cmd/cmd-import-sugarcrm-leads.adoc[]

include::cmd/cmd-import-email-attachment-files.adoc[]

include::cmd/cmd-import-sugarcrm-users.adoc[]

include::cmd/cmd-create-leads.adoc[]

include::cmd/cmd-lead-duplicate-cache.adoc[]

=== Server installatie

==== Github docker login
Eerste keer docker login uitvoeren:
sudo echo <token> | sudo docker login ghcr.io -u <username> --password-stdin

Eerste keer een docker netwerk aanmaken

==== Firewall
- Poort 80 dient open te staan voor traefik (aanmaken van self-signed certificaten)
- Poort 443 dient open te staan privatescan saas

==== Synchronisatie n8n workflows

De eerste keer dien je een gebruiker te registeren.
1. Zet project id in .env file (N8N_PROJECT_ID). Je kunt deze vinden in mysql met select * from project;
2. Maak een API key aan bij je gebruiker en plaats deze in .env file (N8N_API_KEY).


===== Troubleshooting
 *Synchroniseren forceren*
Verwijderen alle bestaande workflows voordat je opnieuw gaat synchroniseren. Je kunt dit doen met een mysql connectie:
mysql -u <DB_MYSQLDB_USER> -p'<DB_MYSQLDB_PASSWORD>' -h 127.0.0.1 -P 3307 <DB_MYSQLDB_DATABASE>
delete from workflow_entity;
delete from shared_workflow;

*Disk full*

Voer analyse uit, commando's:

// # 1. Totale schijfruimte
- df -h
- docker system df
- sudo docker system prune -a --volumes
- sudo du -h /var/lib/docker | sort -hr | head -20

fix met opruimen van docker images:
- docker system prune -a --volumes

(Laatste prune actie leverde 35 GB op)


=== Mail server lokaal opzetten

Het idee is om een eenvoudige mail server lokaal op te zetten. Later gaan we dit met bestaande mail accounts configureren en mail versturen via PostMark.

In de opzet maken wij 2 mail accounts aan:

* user1@test.local -> Hier mail naar toe sturen vanuit CRM, simuleert een patient.
* crm@privatescan.nl -> CRM leest deze mailbox en zal ze verwerken na en commando (Lees later een cronjob om de 5 minuten)

test.local is een gekozen opzet, certificaten zijn hiervoor gemaakt voor communicatie met de mailserver.

zie mailserver in docker-compose.yml.

NOTE: Op dit moment is deze opzet ook uitgerold op de test omgeving.

1. User aanmaken in de mailserver (mag je skippen, werkt out of de box door config file te delen. Zie postfix-accounts.cf):
* docker run --rm -v "$(pwd)/docker/config":/tmp/docker-mailserver \
mailserver/docker-mailserver setup email add user1@test.local test1234
* docker run --rm -v "$(pwd)/docker/config":/tmp/docker-mailserver \
mailserver/docker-mailserver setup email add crm@privatescan.nl test1234
2. rainbow config aanpassen (na opstarten):
* http://localhost:8888/?admin
* Inloggen met admin / 12345

image::rainbow_config.png[]

3 CRM mail binnen halen is (voor nu) handmatig te doen. Door dit commmando te draaien:

----
sail artisan inbound-emails:process
----

Note: Op de test omgeving is dit geautomatiseerd via een cronjob die elke minuut draait.

==== Cronjobs

Het systeem heeft verschillende geautomatiseerde taken die via Laravel's scheduler draaien:

===== Actieve Cronjobs

* **`inbound-emails:process`** - Elke 5 minuten
- Verwerkt inkomende emails vanuit de mailbox
- Zet emails om naar activities en leads

* **`activities:release-overdue`** - Elk uur
- Maakt achterstallige activities vrij voor andere gebruikers
- Voorkomt dat taken vast blijven zitten bij gebruikers

* **`duplicates:refresh-cache --clear`** - Elk uur
- Ververst de lead en person duplicate detection cache
- Zorgt voor optimale performance van het kanban board en person overzicht
- Automatische fallback naar directe queries bij problemen

===== Scheduler Status

[source,bash]
----
# Bekijk alle geplande taken
sail artisan schedule:list

# Test scheduler handmatig
sail artisan schedule:run

# Specifieke taak uitvoeren
sail artisan duplicates:refresh-cache --clear
----

==== Composer update dependencies

Het is van belang om met een frequentie alle dependencies te update (Maandelijks).

1. Voer dit uit: composer update --with-all-dependencies
2. Test het lokaal
3. check de nieuwe lock file in git.

===== Telescope Monitoring

Hoe op te zetten:

1. Installeer Laravel Telescope (indien nog niet gedaan):
- sail composer require laravel/telescope
2, sail artisan telescope:install
3. sail artisan migrate
4. Toegang via /telescope route (alleen in lokale omgeving standaard)
5. in je .env file:
- TELESCOPE_ENABLED=true
6. Plaats je emailadres in TelescopeServiceProvider :

```
protected function gate()
{
    Gate::define('viewTelescope', function ($user) {
    return in_array($user->email, [
    // Vul hier je eigen e-mailadres in
    'jij@domein.nl',
    ]);
    });
}
```

7. sail artisan telescope:publish



include::keycloak-setup.adoc[]

include::logging-setup.adoc[]

