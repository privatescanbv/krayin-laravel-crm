=== Documentatie Update - Call Activities Import

==== Uitgevoerde Wijzigingen

✅ **Git pull uitgevoerd** - Latest changes opgehaald

✅ **MD bestanden verwijderd** - Alle debug/ontwikkel MD bestanden opgeruimd:
* `CALL_ACTIVITIES_IMPORT_EXTENSION.md`
* `IMPORT_FIXES.md` 
* `TEST_FIXES_SUMMARY.md`
* `CALLS_CSTM_FIX.md`
* `FINAL_FIX_SUMMARY.md`
* `DEBUG_CALL_ACTIVITIES.md`

✅ **AsciiDoc documentatie uitgebreid** - `docs/modules/Development/pages/cmd/cmd-import-sugarcrm-leads.adoc` bijgewerkt met:

==== Toegevoegde Secties

===== Tabel Structuur
* **Call Activities tabellen** - Documentatie van `calls` en `calls_cstm` tabellen
* **Veld definities** - Alle relevante velden voor call import

===== Dry-Run Output  
* **Call Activities kolom** toegevoegd aan tabel
* **Call Activities Summary** sectie met details per lead

===== Import Output
* **Call Activities statistieken** in import resultaten
* Imported/skipped counts voor call activities

===== Call Activities Sectie
* **Status Mapping** - SugarCRM naar Krayin status conversie
* **Geïmporteerde Gegevens** - Welke velden worden geïmporteerd
* **JSON Storage** - Custom data opslag uitleg
* **Belangrijke opmerkingen** - Wanneer call activities worden geïmporteerd

==== Resultaat

De bestaande import leads documentatie is nu volledig bijgewerkt met informatie over call activities import functionaliteit. Gebruikers kunnen nu:

1. **Begrijpen** welke tabellen nodig zijn voor call activities
2. **Zien** wat er geïmporteerd wordt in dry-run mode  
3. **Interpreteren** van import output met call statistics
4. **Weten** hoe status mapping werkt
5. **Begrijpen** waar custom data wordt opgeslagen

De documentatie is geïntegreerd in de bestaande structuur zonder aparte secties, waardoor het een natuurlijke uitbreiding vormt van de bestaande import lead functionaliteit.