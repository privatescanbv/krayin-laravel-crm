<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Stub for Webkul Installer AttributeSeeder (package not present in this repo).
 * Extend here if tests require custom attribute definitions.
 */
class TestAttributeSeeder extends Seeder
{
    public function run(): void
    {
        // Intentionally empty — lead attributes are optional for feature tests using factories.
    }
}
